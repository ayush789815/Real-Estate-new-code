const config = {
    darkMode: 'class', // ✅ Must be exactly 'class'
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
};

export default config;